@model IEnumerable<Candidaturas_BO.Models.Curso>

@{
    ViewBag.Title = "Cursos";
}

<h2>Lista de Cursos</h2>

<p class="BoParag">
    @Html.ActionLink("Criar Novo Curso", "Create") | @Html.ActionLink("Inserção em Massa", "MassInsert")
</p>

@using (Html.BeginForm())
{
    <div class="FilterDiv">
        Procurar por nome ou código de curso ou ramo: @Html.TextBox("SearchString")
        <input type="submit" value="Procurar" class="btn btn-default" />
    </div>
}

    <div class="TotalDiv">
        Total de Cursos: @ViewBag.TotalCursos
    </div>

<table class="table">
    <tr>
        <th>
            @Html.ActionLink(HttpUtility.HtmlDecode(Html.DisplayNameFor(model => model.Nome).ToHtmlString()), "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th>
            @Html.ActionLink(HttpUtility.HtmlDecode(Html.DisplayNameFor(model => model.CodigoCurso).ToHtmlString()), "Index", new { sortOrder = ViewBag.CursoSortParm })
        </th>
        <th>
            @Html.ActionLink(HttpUtility.HtmlDecode(Html.DisplayNameFor(model => model.CodigoRamo).ToHtmlString()), "Index", new { sortOrder = ViewBag.RamoSortParm })
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoRamo)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Apagar", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
