@model IEnumerable<Candidaturas_BO.Models.Candidato>

@{
    ViewBag.Title = "Candidatos";
}

<h2>Lista de Candidatos</h2>

@using (Html.BeginForm())
{
<div style="display: flex;margin-top: 20px;">
    Procurar candidatos inscritos entre o seguinte intervalo: &nbsp;
    @Html.TextBox("startDate", null, new { @class = "form-control", type = "date", @style = "width: 12%", @value = "" })
    &nbsp; e &nbsp;
    @Html.TextBox("endDate", null, new { @class = "form-control", type = "date", @style = "width: 12%", @value = "" })
    &nbsp;
    Procurar user com email: \r\n;
    @Html.TextBox("email", null, new { @class = "form-control", type = "text", @style = "width: 12%", @value = "" })
    &nbsp;
    <input type="submit" value="Procurar" class="btn btn-default" />
</div>
}

<div class="TotalDiv">
    Total de Candidatos: @ViewBag.TotalCandidatos
</div>

<table class="table">
    <tr>
        <th>
            Nome Completo
        </th>
        <th>
            Email
        </th>
        <th>
            Data de Nascimento
        </th>
        <th>
            Tipo de Doc
        </th>
        <th>
            Validade de Doc
        </th>
        <th>
            Data de Inscrição
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.DadosPessoais.NomeColoquial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.DadosPessoais.DataNascimento.Day)/@Html.DisplayFor(modelItem => item.User.DadosPessoais.DataNascimento.Month)/@Html.DisplayFor(modelItem => item.User.DadosPessoais.DataNascimento.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.DadosPessoais.TipoDocID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.DadosPessoais.DocumentoValidade.Day)/@Html.DisplayFor(modelItem => item.User.DadosPessoais.DocumentoValidade.Month)/@Html.DisplayFor(modelItem => item.User.DadosPessoais.DocumentoValidade.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.DataCriacao)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", new { id = item.User.ID })
            </td>
        </tr>
    }

</table>
